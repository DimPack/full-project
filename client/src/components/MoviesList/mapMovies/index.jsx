
// import { Link } from 'react-router-dom';
const mapMovies = (movie, handleDelete) => {
    return (
    // <li key={movie.id}>
    //   <Link to={`/users/${movie.id}`}>{movie.email}</Link>
    //   <button onClick={handleDelete(movie.id)}>delete</button>
    // </li>
    <></>
    );
}

export default mapMovies;
